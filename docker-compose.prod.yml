# Production overrides: no source mount, no reload. Use: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
services:
  backend:
    volumes: []
    command: sh -c "python3 init_db.py && uvicorn app.main:app --host 0.0.0.0 --port 8000"